# DNS_Enum_System
This project is a Python-based GUI application for DNS enumeration, enabling users to retrieve A, MX, NS, and TXT records through an intuitive interface. Built with Tkinter and dnspython, it removes the need for command-line tools and includes domain validation, structured output, and result-saving features for basic cybersecurity use.
